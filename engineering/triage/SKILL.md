---
name: triage
description: Triage GitHub issues and bugs through a state machine. Use when user wants to review incoming issues, triage bugs or feature requests, move issues between states, prepare issues for AFK agents, or manage issue workflow. Requires ralph setup (docs/agents/ must exist).
---

# Triage

Move issues through a triage state machine. Reads label vocabulary and issue tracker config from `docs/agents/` (written by ralph).

Every comment posted during triage **must** start with:

```
> *This was generated by AI during triage.*
```

## Reference docs

- [AGENT-BRIEF.md](AGENT-BRIEF.md) — writing durable agent briefs
- [OUT-OF-SCOPE.md](OUT-OF-SCOPE.md) — `.out-of-scope/` knowledge base

## Roles

**Category** (one per issue):
- `bug` — something is broken
- `enhancement` — new feature or improvement

**State** (one per issue):
- `needs-triage` — maintainer needs to evaluate
- `needs-info` — waiting on reporter
- `ready-for-agent` — fully specified, AFK agent can pick up
- `ready-for-human` — needs human implementation
- `wontfix` — will not be actioned

Read `docs/agents/triage-labels.md` for the actual label strings used in this repo. If conflicting state roles exist on an issue, flag it and ask before acting.

## Invocation

User invokes `/triage` in natural language. Examples:

- "Show me what needs attention"
- "Let's look at #42"
- "Move #42 to ready-for-agent"
- "What's ready for agents?"

## Show what needs attention

Read `docs/agents/issue-tracker.md`, then query and present three buckets oldest-first:

1. **Unlabeled** — never triaged
2. **`needs-triage`** — evaluation in progress
3. **`needs-info` with reporter activity since last triage notes** — needs re-evaluation

Show counts and a one-line summary per issue. Let the maintainer pick.

## Triage a specific issue

1. **Gather context.** Read full issue (body, comments, labels, reporter, dates). Parse prior triage notes to avoid re-asking resolved questions. Read `.out-of-scope/*.md` and surface any prior rejection that resembles this issue. Consult `docs/agents/domain.md` for glossary and ADR locations.

2. **Recommend.** State your category and state recommendation with reasoning. Include a brief codebase summary relevant to the issue. Wait for direction.

3. **Reproduce (bugs only).** Before grilling, attempt reproduction: trace the relevant code, run tests or commands. Report: confirmed repro with code path, failed repro, or insufficient detail (strong `needs-info` signal).

4. **Grill (if needed).** If the issue needs fleshing out, ask targeted questions one section at a time.

5. **Apply the outcome:**
   - `ready-for-agent` → post agent brief comment ([AGENT-BRIEF.md](AGENT-BRIEF.md))
   - `ready-for-human` → same structure as agent brief, note why it can't be delegated
   - `needs-info` → post triage notes (template below)
   - `wontfix` (bug) → polite explanation, close
   - `wontfix` (enhancement) → write `.out-of-scope/`, link from comment, close ([OUT-OF-SCOPE.md](OUT-OF-SCOPE.md))
   - `needs-triage` → apply label, optional partial-progress comment

## Quick state override

If user says "move #42 to ready-for-agent", trust them and apply directly. Confirm what you're about to do (label changes, comment, close), then act. Skip grilling. If moving to `ready-for-agent` without a grilling session, ask whether to write an agent brief.

## Needs-info template

```markdown
## Triage Notes

**What we've established so far:**

- point 1
- point 2

**What we still need from you (@reporter):**

- question 1
- question 2
```

Capture everything resolved under "established so far". Questions must be specific and actionable, not "please provide more info".

## Resuming a previous session

If prior triage notes exist, read them, check whether the reporter answered outstanding questions, and present an updated picture. Don't re-ask resolved questions.